import React from 'react';

const AuthStorageContext = React.createContext(); //The defaultValue argument is only used when a component does not have a matching Provider above it in the tree.

export default AuthStorageContext;